#
# Pins the Gradle daemon JVM to Java 21.
# IntelliJ Platform Gradle Plugin 2.x requires Java 21; running the daemon on
# Java 24+ breaks task configuration (TypeNotPresentException in DefaultReportContainer).
# Gradle 8.13 auto-provisions this JVM from locally installed toolchains.
#
toolchainVersion=21
